"use strict";
// Copyright Amazon.com, Inc. or its affiliates. All Rights Reserved.
// SPDX-License-Identifier: Apache-2.0
Object.defineProperty(exports, "__esModule", { value: true });
exports.withIdentityPoolId = withIdentityPoolId;
const credential_providers_1 = require("@aws-sdk/credential-providers");
const authHelper_1 = require("../common/authHelper");
/**
 * Creates an auth helper instance using credentials from Cognito.
 *
 * @param identityPoolId Cognito Identity Pool Id
 */
async function withIdentityPoolId(identityPoolId, options) {
    const region = identityPoolId.split(":")[0];
    const credentialProvider = (0, credential_providers_1.fromCognitoIdentityPool)(Object.assign(Object.assign({}, (options || {})), { identityPoolId, clientConfig: Object.assign(Object.assign({}, (options && options.clientConfig ? options.clientConfig : {})), { region }) }));
    return (0, authHelper_1.buildAuthHelper)(credentialProvider, region);
}
